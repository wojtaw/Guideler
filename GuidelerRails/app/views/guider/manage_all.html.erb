<br>
<br>
Manage all your guiders
<br>
<%= link_to('Create new guider', new_guider_path) %>

  <% guiders.each do |g| %>
    <%= g.id %> / <%= g.name %> <%= link_to('EDIT', editor_path(:guiderID => g.id)) %> | <%= link_to('Delete', delete_guider_path(:guiderID => g.id)) %>

    <br>

  <% end %>